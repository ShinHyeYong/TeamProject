<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="psj.hahaha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="intro.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="intro.FragmentDrawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="intro.SubActivity"
            android:theme="@style/AppTheme" />
        <activity android:name="dbconnected.LogInActivity" />
        <activity android:name="dbconnected.SignInActivity" />
        <activity android:name="listpage.WritePage" />
        <activity android:name="listpage.ContentPage" />
        <activity android:name="dbconnected.ChangeInfoActivity"></activity>
    </application>

</manifest>